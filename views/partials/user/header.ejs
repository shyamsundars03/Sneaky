<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/public/images/sneaky.png" type="image/png">
    <title>SNEAKY - Home</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;

            width: 100%;
        }

        /* Header Styles */
        header {
            background: #000;
            padding: 20px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        .logo {
    display: inline-flex;
    align-items: center;
    padding: 8px 16px;
    border-radius: 8px;
    text-decoration: none;
    color: white;
    font-weight: 800;
    letter-spacing: 1px;
    gap: 10px;
}

.logo-icon {
    width: 24px;
    height: 24px;
}

.logo-text {
    font-size: 20px;
}
        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            text-transform: uppercase;
            font-size: 14px;
        }

        .nav-icons {
            display: flex;
            gap: 20px;
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: white;
            font-size: 24px;
            cursor: pointer;
        }

        /* Hero Section */
        .hero {
            background: #000;
            color: white;
            padding: 40px 0 140px;
            position: relative;
        }

        .hero-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 40px;
        }

        .hero-text {
            flex: 1;
        }

        .hero-text h1 {
            font-size: 48px;
            margin: 20px 0;
        }

        

        .price {
            color: #888;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: white;
            color: black;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        .hero-image {
            flex: 1;
            text-align: right;
        }

        .hero-image img {
            max-width: 100%;
            height: 600px;
            transform: none;
            border-radius: 20px;
        }

       /*features */
.features-wrapper {
    position: absolute;
    left: 50%;
    bottom: -140px;
    transform: translateX(-50%);
    width: 90%;
    max-width: 1200px;
    z-index: 10;
}

.features {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
    padding: 30px;

    background-image: linear-gradient(to bottom, #808080, #000000);
    border-radius: 24px;
    color: white;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
}
        /* Categories */
        .categories {
            padding: 60px 0;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
        }

        .category-card {
            background: white;
            transition: all 0.3s ease;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .category-card img {
            width: 100%;
            height: auto;
            border-radius: 5px;
        }

            /* Men's card */
.category-card:nth-child(1):hover {
    background: linear-gradient(135deg, #d5d2d2, #6c6c6c);
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}

/* Women's card */
.category-card:nth-child(2):hover {
    background: linear-gradient(135deg, #edd0bd, #f29360);
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}

/* Kids's card */
.category-card:nth-child(3):hover {
    background: linear-gradient(135deg, #b3d9ff, rgb(81, 81, 255)
    );
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}







        /* Promo Section */
        .promo {
            background: #ff0000;
            color: white;
            padding: 40px;
            border-radius: 10px;
            margin: 40px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 30px;
        }

        .promo-content h2 {
            font-size: 36px;
            margin-bottom: 10px;
        }

        .promo-buttons {
            display: flex;
            gap: 10px;
            margin-top: 20px;
            flex-wrap: wrap;
        }
                /* Promo section styles */
        .promo img {
            max-width: 250px; /* Smaller promo image */
            height: auto;
            border-radius: 10px;
        }

        .promo-btn {
            padding: 10px 20px;
            background: white;
            color: #ff0000;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        /* Services */
        .services {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            padding: 60px 0;
            text-align: center;
        }

        .service {
            padding: 20px;
        }
        /* Services section styles */
        .service img {
            background: #000; /* Black background for icons */
            padding: 10px;
            border-radius: 8px;
            width: 50px;
            height: 50px;
            object-fit: contain;
            margin-bottom: 15px;
        }

        /* Footer */
        footer {
            background: #000;
            color: white;
            padding: 60px 0 20px;
        }
        .footer-logo {
    display: inline-flex;
    align-items: center;
    padding: 6px 14px;
    border-radius: 8px;
    text-decoration: none;
    color: white;
    font-weight: 800;
    letter-spacing: 1px;
    gap: 8px;
    margin-bottom: 20px;
}

.footer-logo .logo-icon {
    width: 20px;
    height: 20px;
}

.footer-logo .logo-text {
    font-size: 18px;
}


        .footer-content {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
            margin-bottom: 40px;
        }

        .footer-links h3 {
            margin-bottom: 20px;
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 10px;
        }

        .footer-links a {
            color: #888;
            text-decoration: none;
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #333;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .hero-text h1 {
                font-size: 36px;
            }

            .features {
                grid-template-columns: repeat(2, 1fr);
        gap: 20px;
        padding: 30px;
            }

            .footer-content {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .mobile-menu-btn {
                display: block;
            }

            .logo {
        padding: 6px 12px;
    }
    
    .logo-icon {
        width: 20px;
        height: 20px;
    }
    
    .logo-text {
        font-size: 18px;
    }

            .nav-links {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: #000;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: 20px;
            }
            .promo img {
            max-width: 200px; /* Even smaller on mobile */
        }

            .nav-links.active {
                display: flex;
            }

            .hero-content {
                flex-direction: column;
                text-align: center;
            }

            .hero-image {
                order: -1;
            }

            .categories {
                grid-template-columns: repeat(2, 1fr);
            }

            .services {
                grid-template-columns: repeat(2, 1fr);
            }

            .promo {
                flex-direction: column;
                text-align: center;
            }

            .promo img {
                max-width: 100%;
            }

            .hero {
        padding: 30px 0 160px;
    }

    .features-wrapper {
        width: 95%;
        bottom: -100px;
    }




        }

        @media (max-width: 480px) {
            .hero-text h1 {
                font-size: 28px;
            }

            .logo {
        padding: 5px 10px;
    }
    
    .logo-icon {
        width: 18px;
        height: 18px;
    }
    
    .logo-text {
        font-size: 16px;
    }



            .features {
                grid-template-columns: 1fr;
            }

            .categories {
                grid-template-columns: 1fr;
            }

            .services {
                grid-template-columns: 1fr;
            }

            .footer-content {
                grid-template-columns: 1fr;
            }

            .promo-buttons {
                flex-direction: column;
            }

            .nav-icons {
                gap: 10px;
            }


            .features {
        grid-template-columns: 1fr;
        padding: 20px;
    }

    .hero {
        padding: 20px 0 200px;
    }

    .features-wrapper {
        bottom: -150px;
    }

    .container {
        padding-top: 180px;
    }











        }
    </style>
</head>
<body>




    <header>
        <div class="container">
            <nav class="navbar">
                <a href="#" class="logo">
                    <img src="/images/sneaky.png" alt="" class="logo-icon">
                    <span class="logo-text">SNEAKY</span>
                </a>
                <button class="mobile-menu-btn">☰</button>
                <div class="nav-links">
                    <a href="home.html">HOME</a>
                    <a href="about.html">ABOUT</a>
                    <a href="shop.html">SHOP</a>
                    <a href="contact.html">CONTACT</a>
                </div>
                <div class="nav-icons">

                    <a href="/signin" style="color: white; text-decoration: none; text-transform: uppercase; font-size: 14px;">Sign In</a>
                    <a href="/login" style="color: white; text-decoration: none; text-transform: uppercase; font-size: 14px;">Login</a>

                    <a href="#"><img src="/images/icons//search.svg" alt="Search"></a>
                    <a href="#"><img src="/images/icons/person.svg" alt="Account"></a>
                    <a href="#"><img src="/images/icons/cart.svg" alt="Cart"></a>
                    <a href="#"><img src="/images/icons/fav.svg" alt="Wishlist"></a>
                </div>
            </nav>
        </div>
    </header>