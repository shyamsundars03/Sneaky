<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/sneaky.png" type="image/png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <title>SNEAKY</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;

            width: 100%;
        }


        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            right: 0;
            background-color: #000;
            min-width: 120px;
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
            border-radius: 4px;
            overflow: hidden;
        }

        .dropdown-content a {
            color: white;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            font-size: 14px;
            text-align: left;
            transition: background-color 0.3s;
        }

        .dropdown-content a:hover {
            background-color: red;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }
        .dropdown-content form:hover {
            background-color: red;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }
        .account-icon {
            cursor: pointer;
        }

        /* Header Styles */
        header {
            background: #000;
            padding: 20px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        .logo {
    display: inline-flex;
    align-items: center;
    padding: 8px 16px;
    border-radius: 8px;
    text-decoration: none;
    color: white;
    font-weight: 800;
    letter-spacing: 1px;
    gap: 10px;
}

.logo-icon {
    width: 24px;
    height: 24px;
}

.logo-text {
    font-size: 20px;
}
        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            text-transform: uppercase;
            font-size: 14px;
        }

        .nav-icons {
            display: flex;
            gap: 20px;
        }


/* Add this to your existing CSS */
.account-icon {
    display: flex;
    align-items: center; /* Vertically center the icon and text */
    gap: 8px; /* Add some spacing between the icon and text */
    text-decoration: none; /* Remove underline */
    color: white; /* Ensure the text color is white */
}

.account-icon img {
    width: 24px; /* Ensure the icon has a consistent size */
    height: 24px;
}

.account-icon span {
    font-size: 14px; /* Adjust font size as needed */
    color: white; /* Ensure the text color is white */
    text-decoration: none; /* Remove underline */
}



        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: white;
            font-size: 24px;
            cursor: pointer;
        }

        /* Hero Section */
        .hero {
            background: #000;
            color: white;
            padding: 40px 0 140px;
            position: relative;
        }

        .hero-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 40px;
        }

        .hero-text {
            flex: 1;
        }

        .hero-text h1 {
            font-size: 48px;
            margin: 20px 0;
        }

        

        .price {
            color: #888;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: white;
            color: black;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        .hero-image {
            flex: 1;
            text-align: right;
        }

        .hero-image img {
            max-width: 100%;
            height: 600px;
            transform: none;
            border-radius: 20px;
        }

       /*features */
.features-wrapper {
    position: absolute;
    left: 50%;
    bottom: -140px;
    transform: translateX(-50%);
    width: 90%;
    max-width: 1200px;
    z-index: 10;
}

.features {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
    padding: 30px;

    background-image: linear-gradient(to bottom, #808080, #000000);
    border-radius: 24px;
    color: white;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
}

/* Categories */
.categories {
    padding: 60px 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
}

.category-card {
    background: white;
    transition: all 0.3s ease;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    text-decoration: none; /* Remove underline from anchor tags */
    color: inherit; /* Inherit text color */
    display: block; /* Make the anchor tag behave like a block element */
}

.category-card img {
    width: 100%;
    height: auto;
    border-radius: 5px;
}

/* Men's card */
.category-card:nth-child(1):hover {
    background: linear-gradient(135deg, #d5d2d2, #6c6c6c);
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}

/* Women's card */
.category-card:nth-child(2):hover {
    background: linear-gradient(135deg, #d5d2d2, #6c6c6c);
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}

/* Kids's card */
.category-card:nth-child(3):hover {
    background: linear-gradient(135deg, #d5d2d2, #6c6c6c);
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0,0,0,0.1);
}







        /* Promo Section */
        .promo {
            background: #ff0000;
            color: white;
            padding: 40px;
            border-radius: 10px;
            margin: 80px 110px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 30px;
        }

        .promo-content h2 {
            font-size: 36px;
            margin-bottom: 10px;
        }

        .promo-buttons {
            display: flex;
            gap: 10px;
            margin-top: 20px;
            flex-wrap: wrap;
        }
                /* Promo section styles */
        .promo img {
            max-width: 250px; /* Smaller promo image */
            height: auto;
            border-radius: 10px;
        }

        .promo-btn {
            padding: 10px 20px;
            background: white;
            color: #ff0000;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        /* Services */
        .services {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            margin: 80px 110px;
            padding: 60px 0;
            text-align: center;
        }

        .service {
            padding: 20px;
        }
        /* Services section styles */
        .service img {
            background: #000; /* Black background for icons */
            padding: 10px;
            border-radius: 8px;
            width: 50px;
            height: 50px;
            object-fit: contain;
            margin-bottom: 15px;
        }

        /* Footer */
        footer {
            background: #000;
            color: white;
            padding: 60px 0 20px;
        }
        .footer-logo {
    display: inline-flex;
    align-items: center;
    padding: 6px 14px;
    border-radius: 8px;
    text-decoration: none;
    color: white;
    font-weight: 800;
    letter-spacing: 1px;
    gap: 8px;
    margin-bottom: 20px;
}

.footer-logo .logo-icon {
    width: 20px;
    height: 20px;
}

.footer-logo .logo-text {
    font-size: 18px;
}


        .footer-content {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
            margin-bottom: 40px;
        }

        .footer-links h3 {
            margin-bottom: 20px;
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 10px;
        }

        .footer-links a {
            color: #888;
            text-decoration: none;
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #333;
        }



  /* Hero Section */
  .shop-hero {
            position: relative;
            background-color: #000;
            color: #fff;
            padding: 30px 0px;
            text-align: center;
            flex-direction: row;
        }

        .shop-hero-content h1 {
            font-size: 3.5rem;
            margin-bottom: 10px;
        }
        .shop-hero-content {
    display: flex;
    flex-direction: column; /* Changed to column to stack items vertically */
    justify-content: center;
    align-items: center;
    gap: 10px; /* Added gap between elements */
}

        .breadcrumb {
            font-size: 0.9rem;
            opacity: 0.8;
        }
        .breadcrumb a {
            color: red;
            font-weight: bold;
        }

        .hero-wave {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 50px;
            background-size: cover;
            
        }

        /* Shop Container */
        .shop-container {
            display: flex;
            padding: 80px;
            gap: 30px;
        }

        /* Sidebar Styles */
        .shop-sidebar {
            width: 280px;
            flex-shrink: 0;
        }

        .filter-section {
            margin-bottom: 30px;
        }

        .filter-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 10px;
        }

        .filter-header h4 {
            font-size: 1rem;
        }
        .single-filter-header h4 {
            font-size: 1.2rem;
        }
        .rating{
            color: yellow;
            border: #000;
        }
        .rating span {
            color: #000;
        }

        .filter-options {
    display: flex;
    flex-direction: column;
    gap: 10px;
    color: #000000;
}



        .filter-option {
            display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.9em;
    color: #000000;
        }

        .filter-option input[type="checkbox"] {
            margin-right: 10px;
            position: relative;
            width: 16px;
            height: 16px;
            cursor: pointer;
        }

        .price-range {
            margin-top: 15px;
        }

        .range-inputs {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .range-inputs input {
            width: 45%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .range-slider {
            width: 100%;
            margin-bottom: 10px;
        }

        .apply-button {
            width: 100%;
            padding: 8px;
            background-color: #f5f5f5;
            border: 1px solid #ddd;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .apply-button:hover {
            background-color: #e0e0e0;
        }
        .filter-button {
            width: 100%;
            padding: 8px;
            background-color: red;
            border: none;
            color: white;
            border-radius: 4px;
            transition: background-color 0.3s;
            cursor: pointer;
            margin-bottom: 10px;
        }

        .shop-search {
            width: 100%;
            padding: 8px;
            background-color: red;
            border: none;
            color: white;
            border-radius: 4px;
            transition: background-color 0.3s;
            cursor: pointer;
            margin-bottom: 10px;
        }


        .size-options {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .size-option {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            transition: all 0.3s;
        }

        .size-option.active,
        .size-option:hover {
            background-color: #000;
            color: #fff;
        }

        .stars {
            color: #ffc107;
        }

        /* Products Container */
        .products-container {
            flex: 1;
        }

        .products-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .products-view-options {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .sort-select {
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        .view-options button {
            padding: 5px;
            font-size: 1.1rem;
        }

        .view-options button.active {
            color: #000;
        }

        /* Products Grid */
        .products-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .product-card {
            border: 1px solid #f0f0f0;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .product-image img {
            width: 100%;
            height: auto;
            display: block;
        }

        .product-info {
            padding: 15px;
        }

        .product-price {
            margin-bottom: 10px;
        }

        .current-price {
            font-weight: bold;
            color: red;
            font-size: 1.1rem;
        }
        .related-current-price {
            text-decoration: line-through;
            color: #999;
            margin-left: 5px;
            font-size: 0.9rem;
        }
        .offer-price {
            font-weight: bold;
            color: red;
            font-size: 1.1rem;
        }
        
        .original-price {
            text-decoration: line-through;
            color: #999;
            margin-left: 5px;
            font-size: 0.9rem;
        }

        .related-original-price {
            font-weight: bold;
            color: red;
            font-size: 1.1rem;
        }

        .product-name {
            font-size: 0.9rem;
            margin-bottom: 15px;
        }

        .product-actions {
            display: flex;
            justify-content: space-between;
            gap: 10px;
        }

        .add-to-cart {
            padding: 8px 15px;
            background-color: #e92c2c;
            color: #fff;
            border: none;
            border-radius: 4px;
            transition: background-color 0.3s;
            cursor: pointer;
        }

        .add-to-cart:hover {
            background-color: #c71f1f;
        }

        .add-to-wishlist {
            padding: 8px 12px;
    background: #f5f5f5;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s;
    display: flex;
    align-items: center;
    justify-content: center;
        }
        .add-to-wishlist i {
    color: #666;
    font-size: 16px;
}

        .add-to-wishlist:hover {
            background-color: #d9d7d7;
        }

        /* Pagination */
        .pagination {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 30px;
        }

        .pagination-info {
            font-size: 0.9rem;
            color: #666;
        }

        .pagination-controls {
            display: flex;
            gap: 5px;
        }

        .pagination-controls a {
            padding: 5px 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            transition: all 0.3s;
            background-color: #f5f5f5;
    color: #333;
    text-decoration: none;
        }

        .pagination-controls a.active,
        .pagination-controls a:hover,
        .pagination-controls .pagination-prev:hover,
.pagination-controls .pagination-next:hover {
            background-color: #e92c2c;
            color: #fff;

        }


        /* SIngele Product */

/* Product Details Section */

.hero-hero-content{
    display: flex;
    flex-direction: column; /* Changed to column to stack items vertically */
    justify-content: center;
    align-items: center;
    gap: 10px; /* Added gap between elements */
}

.product-section {
    padding: 40px 0;
}

.product-container {
    display: flex;
    gap: 40px;
}

.product-images {
    flex: 1;
}

.main-image {
    width: 100%;
    height: 400px;
    object-fit: cover;
    border-radius: 8px;
}

.thumbnail-container {
    display: flex;
    gap: 10px;
    margin-top: 10px;
}

.thumbnail {
    width: 80px;
    height: 80px;
    object-fit: cover;
    border-radius: 4px;
    cursor: pointer;
}

.product-info {
    flex: 1;
}

.product-info h2 {
    font-size: 2em;
    margin-bottom: 10px;
}

.product-info .price {
    font-size: 1.5em;
    font-weight: bold;
    color: red;
    margin-bottom: 20px;
}

.product-info .discount {
    font-size: 0.8em;
    color: #999;
    text-decoration: line-through;
    margin-left: 10px;
}

.product-info p {
    font-size: 1em;
    color: #666;
    margin-bottom: 20px;
}

.size-selector {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
}

.size-btn {
    padding: 8px 12px;
    border: 1px solid #ddd;
    border-radius: 4px;
    background-color: #f5f5f5;
    cursor: pointer;
}

.size-btn:hover {
    background-color: #e0e0e0;
}

.quantity-selector {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 20px;
}

.quantity-btn {
    padding: 8px 12px;
    border: 1px solid #ddd;
    border-radius: 4px;
    background-color: #f5f5f5;
    cursor: pointer;
}

.quantity-btn:hover {
    background-color: #e0e0e0;
}

.action-buttons {
    display: flex;
    gap: 10px;
}

.buy-now {
    padding: 12px 24px;
    background-color: red;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

/* .buy-now:hover {
    background-color: #2980b9;
} */

/* Related Products Section */
.related-products {
    padding: 40px 0;
}

.related-products h2 {
    font-size: 1.5em;
    margin-bottom: 20px;
}

.products-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
}

.product-card {
    border: 1px solid #ddd;
    border-radius: 8px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.product-image img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.product-info {
    padding: 15px;
}

.product-price {
    font-size: 1.2em;
    color: #333;
    margin-bottom: 10px;
}

.current-price {
    font-weight: bold;
    color: red;
}

.original-price {
    text-decoration: line-through;
    color: #999;
    margin-left: 10px;
}

.product-name {
    font-size: 1.1em;
    margin: 10px 0;
    color: #333;
}

.product-actions {
    display: flex;
    justify-content: center;
    gap: 10px;
}

.add-to-cart, .add-to-wishlist {
    padding: 8px 16px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.9em;
}
.add-to-cart-btn {
    padding: 8px 16px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.9em;
}


.add-to-cart {
    background-color: red;
    color: white;
}
.add-to-cart-btn {
    background-color: red;
    color: white;
}

.add-to-wishlist {
    background-color: rgb(255, 255, 255);
    color: white;
}
.add-to-wishlist:hover {
    background-color: rgb(194, 192, 192);
    color: white;
}




 /* About Content */
 .about-content {
            background-color: #fff;
            color: #000;
            padding: 50px 5%;
            display: flex;
            gap: 40px;
        }

        .about-image {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f0f0;
            border-radius: 10px;
            padding: 20px;
        }

        .about-image img {
            max-width: 100%;
            height: auto;
        }

        .about-text {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .about-heading {
            font-size: 32px;
            font-weight: bold;
        }

        .about-paragraph {
            line-height: 1.6;
        }

        .dropdown {
            border-bottom: 1px solid #ddd;
            padding-bottom: 15px;
        }

        .dropdown-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            padding: 10px 0;
        }
        .hero-title{
            font-size: 3.5rem;
            margin-bottom: 10px;
    display: flex;
    flex-direction: column; /* Changed to column to stack items vertically */
    justify-content: center;
    align-items: center;
    gap: 10px; /* Added gap between elements */
}

.size-selector {
    display: flex;
    gap: 10px; /* Space between buttons */
    flex-wrap: wrap; /* Wrap buttons to the next line if they overflow */
}

.size-btn {
    padding: 10px 15px;
    border: 1px solid #ccc;
    border-radius: 5px;
    background-color: #f9f9f9;
    cursor: pointer;
    font-size: 14px;
    text-transform: uppercase; /* Convert text to uppercase (e.g., "xs" to "XS") */
    transition: background-color 0.3s, border-color 0.3s;
}

.size-btn:hover {
    background-color: #e0e0e0;
    border-color: #999;
}

.size-btn.active {
    background-color: #000000; /* Highlight color for selected size */
    color: white;
    border-color: #000000;
}


.about-hero {
            position: relative;
            background-color: #000;
            color: #fff;
            padding: 30px 0px;
            text-align: center;
            flex-direction: row;
        }

        .contact-hero {
            position: relative;
            background-color: #000;
            color: #fff;
            padding: 30px 0px;
            text-align: center;
            flex-direction: row;
        }


        .dropdown-content form{
    color: white;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    font-size: 14px;
    text-align: left;
    transition: background-color 0.3s;
}

/* profile.ejs */

 /* Banner styles */
 .address-banner {
            background-color: #000;
            color: white;
            padding: 50px 0;
            text-align: center;
        }
        
        .address-banner h1 {
            font-size: 2.5rem;
            letter-spacing: 2px;
        }
        
        /* Address page styles */
        .address-section {
            background-color: #fff;
            border-radius: 20px 20px 0 0;
            margin-top: -20px;
            padding: 50px 0;
        }
        
        .address-content {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        
        .account-sidebar {
            flex: 1;
            min-width: 250px;
            max-width: 300px;
            background: #fff;
            border-radius: 15px;
            border: 2px solid rgb(181, 181, 181);
            padding: 30px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }
        
        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: block;
            object-fit: cover;
        }
        
        .account-name {
            text-align: center;
            margin-bottom: 10px;
            font-size: 1.4rem;
        }
        
        .account-email {
            text-align: center;
            color: #777;
            margin-bottom: 30px;
            font-size: 0.9rem;
        }
        
        .sidebar-menu {
            list-style: none;
        }
        
        .sidebar-menu li {
            margin-bottom: 15px;
        }
        
        .sidebar-menu a {
            display: flex;
            align-items: center;
            color: #555;
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .sidebar-menu a:hover {
            color: #ff0000;
        }
        
        .sidebar-menu i {
            margin-right: 10px;
            width: 20px;
        }
        
        /* Address Details */
        .address-details {
            flex: 2;
            min-width: 300px;
        }
        
        .address-details h2 {
            margin-bottom: 30px;
            font-size: 1.8rem;
        }
        
        /* Address Cards */
        .address-list {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        
        .add-address-btn {
            display: block;
            width: 100%;
            padding: 15px;
            background-color: #ff0000;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            margin-bottom: 30px;
            text-decoration: none;
        }
        
        .add-address-btn:hover {
            background-color: #cc0000;
        }
        
        .address-card {
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            position: relative;
            margin-bottom: 15px;
        }
        
        .address-label {
            display: inline-block;
            background-color: #f5f5f5;
            padding: 4px 10px;
            border-radius: 3px;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .address-text {
            margin-bottom: 10px;
        }
        
        .address-actions {
            position: absolute;
            top: 20px;
            right: 20px;
            display: flex;
            gap: 10px;
        }
        
        .address-action-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 30px;
            height: 30px;
            background-color: #f1f1f1;
            border: 1px solid #ddd;
            border-radius: 5px;
            cursor: pointer;
        }
        
        .address-action-btn.edit {
            color: #555;
        }
        
        .address-action-btn.delete {
            color: #f44336;
        }
        
        /* Profile Image Styles */
        .profile-image-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 20px;
        }

        .profile-image {
            position: relative;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            overflow: hidden;
            margin-bottom: 15px;
            border: 3px solid #f5f5f5;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .profile-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .address-id{
            color: red;
            font-weight: bold;
        }

        /* Cart.ejs */
        .cart-content {
            background-color: #fff;
            color: #000;
            padding-top: 15px;
            padding-left: 160px;
            padding-right: 160px;
            flex-direction: column;
            padding-bottom: 50px;


        }

        .cart-items-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .cart-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;

        }

        .cart-table th {
            text-align: left;
            padding: 10px 0;
            border-bottom: 1px solid #ddd;
            font-size: 18px;
        }

        .cart-table td {
            padding: 20px 0;
            border-bottom: 1px solid #ddd;
            vertical-align: middle;
        }

        .cart-product-info {
            display: flex;
            align-items: center;
        }

        .cart-product-img {
            width: 80px;
            height: 80px;
            margin-right: 15px;
        }

        .cart-product-details {
            display: flex;
            flex-direction: column;
        }

        .cart-product-name {
            font-size: 14px;
            margin-bottom: 5px;
        }

        .cart-product-price {
            font-size: 14px;
            color: #666;
        }

        .cart-quantity-control {
            display: flex;
            align-items: center;
        }

        .cart-quantity-btn {
            width: 25px;
            height: 25px;
            background-color: #f2f2f2;
            border: 1px solid #ddd;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
        }

        .cart-quantity-input {
            width: 40px;
            height: 25px;
            border: 1px solid #ddd;
            text-align: center;
            margin: 0 5px;
        }

        .cart-delete-btn {
            color: #ff0000;
            cursor: pointer;
            font-size: 16px;
            border: none;
        }

        /* Cart Summary */
        .cart-summary {
            width: 100%;
            max-width: 300px;
            margin-left: auto;
            border: 1px solid #ddd;
            border-radius: 4px;
            padding: 20px;

        }

        .cart-total-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .cart-summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .cart-summary-label {
            font-size: 14px;
        }

        .cart-summary-value {
            font-size: 14px;
            font-weight: bold;
        }

        .total-row {
            margin-top: 15px;
            padding-top: 15px;
            border-top: 1px solid #ddd;
        }

        .checkout-btn {
            display: block;
            width: 100%;
            padding: 12px;
            background-color: #ff0000;
            color: #fff;
            text-align: center;
            border: none;
            border-radius: 4px;
            margin-top: 20px;
            cursor: pointer;
            font-weight: bold;
        }
        .cart-checkout-btn {
            display: block;
            width: 100%;
            padding: 12px;
            background-color: #ff0000;
            color: #fff;
            text-align: center;
            border: none;
            border-radius: 4px;
            margin-top: 20px;
            cursor: pointer;
            font-weight: bold;
        }

        .cart-hero {
            background-color: #000;
            height: 200px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .cart-container {
            margin-top: -25px;
            max-width: auto;
            background-color: #000;
            color: #fff;
            overflow: hidden;

        }
        .cart-hero h1 {
            font-size: 40px;
            letter-spacing: 2px;
        }

        .cart-wave {
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 50px;
            background-color: #fff;
            border-radius: 100% 100% 0 0;
        }

        /* checkout1.ejs */


        .checkout-main-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .checkout-header {
            background: #000;
            color: #fff;
            padding: 3rem 0;
            text-align: center;
            position: relative;
            font-size: 1.3rem;
        }

        .wave-divider {
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 100%;
            height: 50px;
            background: #fff;
            border-radius: 100% 100% 0 0;
        }

        .checkout-container {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
            padding: 2rem;
            background: #fff;
            border-radius: 8px;
            margin-top: -2rem;
            position: relative;
            z-index: 1;
        }

        .checkout-progress-bar {
            display: flex;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .checkout-progress-item {
            flex: 1;
            padding: 1rem;
            border-bottom: 2px solid #eee;
        }

        .checkout-progress-item.active {
            border-bottom-color: #ff0000;
        }

        .address-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin: 1rem 0;
        }

        .address-card {
            border: 1px solid #eee;
            padding: 1rem;
            border-radius: 8px;
            cursor: pointer;
        }

        .address-card.selected {
            border-color: #ff0000;
        }

        .shipping-methods {
            display: grid;
            gap: 1rem;
            margin: 1rem 0;
        }

        .shipping-option {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            border: 1px solid red;
            border-radius: 8px;
        }

        .order-summary {
            background: #f9f9f9;
            padding: 1.5rem;
            border-radius: 8px;
        }

        .order-summary h2 {
            padding-bottom: 20px;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #eee;
        }

        .checkout-total {
            color: #ff0000;
            font-weight: bold;
            font-size: 1.2rem;
        }
        .checkout-btn {
            display: inline-block;
            width: 150px;
            padding: 12px 30px;
            background: red;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }



        /* Responsive ==============================================================================================
        ====================================================
        ===========================================================================
        ===================================================
        ===========================================================================
        ======================Design */




        @media (max-width: 1024px) {
            .hero-text h1 {
                font-size: 36px;
            }

            .features {
                grid-template-columns: repeat(2, 1fr);
                gap: 20px;
                padding: 30px;
            }

            .footer-content {
                grid-template-columns: repeat(2, 1fr);
            }
            .products-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .mobile-menu-btn {
                display: block;
            }

            .logo {
        padding: 6px 12px;
    }
    
    .logo-icon {
        width: 20px;
        height: 20px;
    }
    
    .logo-text {
        font-size: 18px;
    }

            .nav-links {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: #000;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: 20px;
            }
            .promo img {
            max-width: 200px; 
        }

            .nav-links.active {
                display: flex;
            }

            .hero-content {
                flex-direction: column;
                text-align: center;
            }

            .hero-image {
                order: -1;
            }

            .categories {
                grid-template-columns: repeat(2, 1fr);
            }

            .services {
                grid-template-columns: repeat(2, 1fr);
            }

            .promo {
                flex-direction: column;
                text-align: center;
            }

            .promo img {
                max-width: 100%;
            }

            .hero {
        padding: 30px 0 160px;
    }

    .features-wrapper {
        width: 95%;
        bottom: -100px;
    }





    .shop-container {
                flex-direction: column;
            }

            .shop-sidebar {
                width: 100%;
            }

            .main-navigation ul {
                display: none;
            }

            .products-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }
            .about-content {
                flex-direction: column;
            }

            .about-image, .about-text {
                width: 100%;
            }

            /* profile.ejs */
            .address-content {
                flex-direction: column;
            }
            
            .account-sidebar {
                max-width: 100%;
            }

            /* cart.ejs */

            .cart-content {
                padding: 20px;
            }

            .product-img {
                width: 60px;
                height: 60px;
            }
            .cart-table thead {
                display: none;
            }

            .cart-table tbody tr {
                display: block;
                margin-bottom: 20px;
                border-bottom: 2px solid #ddd;
            }

            .cart-table td {
                display: block;
                text-align: right;
                padding: 10px 0;
                border-bottom: 1px solid #eee;
            }

            .cart-table td:before {
                content: attr(data-label);
                float: left;
                font-weight: bold;
                text-transform: uppercase;
                font-size: 12px;
            }

            .cart-table td:last-child {
                border-bottom: 0;
            }

            .product-info {
                justify-content: flex-end;
            }


            /* checkout1.ejs */
            .checkout-container {
                grid-template-columns: 1fr;
            }
            .progress-bar {
                flex-direction: column;
            }

        }

        @media (max-width: 480px) {
            .hero-text h1 {
                font-size: 28px;
            }

            .logo {
        padding: 5px 10px;
    }
    
    .logo-icon {
        width: 18px;
        height: 18px;
    }
    
    .logo-text {
        font-size: 16px;
    }



            .features {
                grid-template-columns: 1fr;
            }

            .categories {
                grid-template-columns: 1fr;
            }

            .services {
                grid-template-columns: 1fr;
            }

            .footer-content {
                grid-template-columns: 1fr;
            }

            .promo-buttons {
                flex-direction: column;
            }

            .nav-icons {
                gap: 10px;
            }


            .features {
        grid-template-columns: 1fr;
        padding: 20px;
    }

    .hero {
        padding: 20px 0 200px;
    }

    .features-wrapper {
        bottom: -150px;
    }

    .container {
        padding-top: 180px;
    }

    .products-grid {
                grid-template-columns: 1fr;
            }
            .pagination {
                flex-direction: column;
                gap: 15px;
                align-items: flex-start;
            }


            /* profile.ejs */

            .address-card {
                padding: 15px;
            }
            
            .address-actions {
                position: static;
                margin-top: 15px;
                justify-content: flex-end;
            }


        }





    </style>
</head>
<body>
    <header>
        <div class="container">
            <nav class="navbar">
                <a href="#" class="logo">
                    <img src="/images/sneaky.png" alt="" class="logo-icon">
                    <span class="logo-text">SNEAKY</span>
                </a>
                <button class="mobile-menu-btn">☰</button>
                <div class="nav-links">
                    <a href="/">HOME</a>
                    <a href="/about">ABOUT</a>
                    <a href="/shop">SHOP</a>
                    <a href="/contact">CONTACT</a>
                </div>
                <div class="nav-icons">
                    <!-- Conditionally render SignUp and SignIn links -->
                    <!-- <% if (!user) { %>
                        <a href="/signup" style="color: white; text-decoration: none; text-transform: uppercase; font-size: 14px;">SignUp</a>
                        <a href="/signin" style="color: white; text-decoration: none; text-transform: uppercase; font-size: 14px;">SignIn</a>
                    <% } %> -->

                    <!-- <a href="#"><img src="/images/icons/search.svg" alt="Search"></a> -->
                    <div class="dropdown">
                        <!-- Conditionally render account icon and dropdown -->
                        <% if (user) { %>
                            <a href="/account" class="account-icon">
                                   
                                <img src="/images/icons/person.svg" alt="Account">
                                <span ><%= user.name %></span>
                            </a>
                            <div class="dropdown-content">
                                <a href="/profile">Profile</a>
                                <form action="/logout" method="post">
                                    <button type="submit" style="background: none; border: none; cursor: pointer; color: inherit;  font-size: 14px;">
                                        Sign Out
                                    </button>
                                   
                                </form>
                            </div>
                        <% } else { %>
                            <a href="/signin" class="account-icon">
                                <img src="/images/icons/person.svg" alt="Account">
                            </a>
                            <div class="dropdown-content">
                                <a href="/signin">Login</a>
                            </div>
                        <% } %>
                    </div>
                     <!-- Conditionally render cart and wishlist icons -->
                     <% if (user) { %>
                        <a href="/cart"><img src="/images/icons/cart.svg" alt="Cart"></a>
                        <a href="/wishlist"><img src="/images/icons/fav.svg" alt="Wishlist"></a>
                    <% } else { %>
                        <a href="/signin"><img src="/images/icons/cart.svg" alt="Cart"></a>
                        <a href="/signin"><img src="/images/icons/fav.svg" alt="Wishlist"></a>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>

    <script>
        // Optional: Add JavaScript to handle dropdown on mobile devices
        document.querySelector('.account-icon').addEventListener('click', function (e) {
            e.preventDefault();
            const dropdown = document.querySelector('.dropdown-content');
            dropdown.style.display = dropdown.style.display === 'block' ? 'none' : 'block';
        });

        // Close dropdown when clicking outside
        document.addEventListener('click', function (e) {
            if (!e.target.closest('.dropdown')) {
                document.querySelector('.dropdown-content').style.display = 'none';
            }
        });
    </script>
</body>