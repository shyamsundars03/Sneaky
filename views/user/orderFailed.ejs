<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Placed Successfully - SNEAKY</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        body {
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .container {
            background-color: #fff;
            padding-top: 30px;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 4px 2px 4px 2px rgba(0, 0.1, 0.1, 0.1);
            text-align: center;
            max-width: 800px;
            width: 100%;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #000;
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #000;
        }

        p {
            font-size: 1rem;
            margin-bottom: 20px;
            color: #555;
        }

        .transaction-id {
            font-size: 1.2rem;
            font-weight: bold;
            color: #ff0000;
            margin-bottom: 30px;
        }

        .buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 30px;
        }

        .btn {
            background-color: #ff0000;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #d00000;
        }

        .footer {
            background-color: #000;
            color: #fff;
            padding: 20px;
            width: 100%;
            text-align: center;
            margin-top: auto;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 10px;
        }

        .footer-links a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
        }

        .footer-links a:hover {
            color: #ff0000;
        }

        .copyright {
            font-size: 0.9rem;
            color: #aaa;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }

            h2 {
                font-size: 1.2rem;
            }

            .buttons {
                flex-direction: column;
                gap: 10px;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>SNEAKY</h1>
        <h2>Payment Failed</h2>
        <p>Order ID: <span class="transaction-id"><%= transactionId %></span></p>
        <p>Your order was created but payment failed.</p>
        <div class="buttons">
            <a href="/orders/<%= orderId %>/retry" class="btn">Retry Payment</a>
            <a href="/orders/<%= orderId %>" class="btn">View Order</a>
        </div>
    </div>

</body>
</html>